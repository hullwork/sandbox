apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

# Shared, explicitly non-production dependencies for local clusters.
resources:
  - object-store.yaml
  - metrics-server.yaml

patches:
  - path: control-plane-sqlite.yaml
  - path: workspace-storage-local-path.yaml
