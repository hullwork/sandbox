apiVersion: ceph.rook.io/v1
kind: CephCluster
metadata:
  name: rook-ceph
  namespace: rook-ceph
spec:
  cephVersion:
    # v20.2.4, pinned to the multi-architecture index digest (amd64 and
    # arm64) so the tag cannot be re-pointed underneath the local profile.
    # rook/loop-device.yaml carries the same digest; move both together.
    image: quay.io/ceph/ceph:v20.2.4@sha256:6bb1c8a42fbc0bf87938946990b65174466997bc11c31eb5a323225a779fd8f9
    allowUnsupported: false
  dataDirHostPath: /var/lib/rook
  mon:
    count: 1
    allowMultiplePerNode: true
  mgr:
    count: 1
    allowMultiplePerNode: true
  dashboard:
    enabled: false
  crashCollector:
    disable: true
  storage:
    useAllNodes: true
    useAllDevices: false
    # Rook v1.20 deliberately excludes loop devices from deviceFilter matches.
    # The local preparation DaemonSet deterministically owns this exact device.
    devices:
      - name: /dev/loop0
  disruptionManagement:
    managePodBudgets: false
  priorityClassNames:
    all: system-cluster-critical
---
apiVersion: ceph.rook.io/v1
kind: CephObjectStore
metadata:
  name: object-store
  namespace: rook-ceph
spec:
  metadataPool:
    failureDomain: host
    replicated:
      size: 1
      requireSafeReplicaSize: false
  dataPool:
    failureDomain: host
    replicated:
      size: 1
      requireSafeReplicaSize: false
  preservePoolsOnDelete: true
  gateway:
    port: 80
    instances: 1
    resources:
      requests:
        cpu: 100m
        memory: 256Mi
      limits:
        cpu: "2"
        memory: 2Gi
---
apiVersion: ceph.rook.io/v1
kind: CephObjectStoreUser
metadata:
  name: sandbox-runtime
  namespace: rook-ceph
spec:
  store: object-store
  displayName: Sandbox Runtime
  quotas:
    maxBuckets: 3
